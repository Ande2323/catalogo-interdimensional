.DS_Store
Thumbs.db
node_modules/

# tokens y secretos: nunca deben subirse
*token*.txt
*.token
secretos*

# Configuración local de desarrollo (servidor de previsualización)
.claude/*
# ...pero las skills del proyecto sí se comparten: así están disponibles en
# cualquier sesión que clone el repositorio, incluidas las de Claude en la web.
!.claude/skills/
